# Replication prompt — Project 12

Replicate the TimePulse forecasting experiment with a reproducible local workflow. Generate a trend + weekly seasonality series, add realistic anomalies, create lag/rolling features, use chronological validation, compare a seasonal-naive baseline with a tree-based forecaster, and report MAE/RMSE/MAPE. Add one improvement over the reference and explain it.

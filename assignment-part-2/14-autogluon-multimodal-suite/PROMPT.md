# Replication prompt — Project 14

Replicate the AutoGluon multimodal suite idea without requiring heavyweight foundation-model downloads. Build a small product valuation example with three modalities: TF-IDF text, numeric image descriptors, and tabular metadata. Train separate modality models, then compare equal-weight late fusion against a learned stacking model. Include an ablation showing the contribution of each modality.
